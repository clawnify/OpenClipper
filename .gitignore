node_modules
dist
.wrangler
.mf

# Local secrets (CLAWNIFY_TOKEN etc.) — never commit or deploy
.dev.vars
.dev.vars.*

*.log
.DS_Store

# Clawnify deploy state (contains app_token) — never commit
.clawnify
.clawnify/

# Generated by `clawnify dev` (into .clawnify/) and by the deploy pipeline — never hand-written
wrangler.toml
wrangler.jsonc

# Internal design-system guidance for the builder/agent — never published
DESIGN.md
